@model IEnumerable<Bioterio.Models.Localcaptura>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Localidade)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Latitude)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Longitude)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.Concelho)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Distrito)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Localidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Latitude)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Longitude)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Concelho.NomeConcelho)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Distrito.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdLocalCaptura">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdLocalCaptura">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdLocalCaptura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
